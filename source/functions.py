def create_product(name, quantity, price):
    """
    Creates a new product in the inventory and its ID
    """
    prodcut = {
        "name" : name,
        "quantity" : quantity,
        "price" : price
    }
    return prodcut    